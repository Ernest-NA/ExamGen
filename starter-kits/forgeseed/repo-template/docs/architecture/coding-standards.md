# Coding standards

- Prefer small, reviewable changes.
- Use clear names and avoid magic strings.
- Preserve existing project conventions unless there is a clear reason to change them.
- Keep public interfaces stable unless the task explicitly allows a breaking change.
- Update tests and docs when behavior changes.
